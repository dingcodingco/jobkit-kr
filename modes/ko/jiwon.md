# 모드: jiwon -- 지원서 작성 live assistant

후보자가 Chrome에서 지원서 폼을 작성하는 순간 사용하는 interactive mode입니다. 화면에 보이는 내용을 읽고, 이전에 평가한 채용 공고의 context를 불러온 뒤, 폼의 각 질문에 맞는 개인화 답변을 생성합니다.

## 사전 조건

- **Visible Playwright가 가장 이상적**: visible mode에서는 후보자가 브라우저를 직접 볼 수 있고, Claude가 페이지와 상호작용할 수 있습니다.
- **Playwright 없이 진행하는 경우**: 후보자가 스크린샷을 공유하거나 질문을 직접 붙여넣습니다.

## Workflow

```text
1. DETECT      -> 활성 Chrome 탭 읽기 (스크린샷/URL/title)
2. IDENTIFY    -> 페이지에서 회사 + 역할 추출
3. SEARCH      -> reports/ 안의 기존 report와 매칭
4. LOAD        -> 전체 report + 블록 G(있다면) 읽기
5. COMPARE     -> 화면의 역할이 평가한 역할과 같은지 확인. 바뀌었다면 알림
6. ANALYZE     -> 화면에 보이는 모든 지원서 질문 식별
7. GENERATE    -> 각 질문에 대해 개인화 답변 생성
8. PRESENT     -> 복사/붙여넣기 가능한 형식으로 답변 표시
```

## Step 1 -- 공고 감지

**Playwright 사용 시:** 활성 페이지 snapshot을 뜹니다. title, URL, 보이는 내용을 읽습니다.

**Playwright 없이 진행 시:** 후보자에게 다음 중 하나를 요청합니다.
- 지원서 폼 스크린샷 공유 (Read tool이 이미지를 읽을 수 있음)
- 또는 지원서 질문을 텍스트로 붙여넣기
- 또는 회사 + 역할을 알려주면 context를 검색

## Step 2 -- context 식별 및 불러오기

1. 페이지에서 회사명과 역할명을 추출합니다.
2. `reports/`에서 회사명으로 검색합니다 (case-insensitive grep).
3. 매칭되는 report가 있으면 전체 report를 불러옵니다.
4. 블록 G가 있으면 이전 답변 초안을 base로 불러옵니다.
5. 매칭되는 report가 없으면 후보자에게 알리고 빠른 auto-pipeline 실행을 제안합니다.

## Step 3 -- 역할 변경 감지

화면의 역할이 평가한 역할과 다르면:
- **후보자에게 알림**: "역할이 [X]에서 [Y]로 바뀐 것 같습니다. 새 역할로 다시 평가할까요, 아니면 새 title에 맞춰 답변만 조정할까요?"
- **조정 선택 시**: 재평가 없이 새 역할에 맞춰 답변을 조정합니다.
- **재평가 선택 시**: 전체 A-G 평가를 실행하고, report를 업데이트하고, 블록 G를 다시 생성합니다.
- **tracker 업데이트**: 필요한 경우 `applications.md`의 역할명을 수정합니다.

## Step 4 -- 지원서 질문 분석

화면에 보이는 모든 질문을 식별합니다.
- 자유 입력 필드 (커버레터, "왜 이 역할인가요", 지원 동기 등)
- 드롭다운 (어디서 공고를 봤는지, 근무 자격 등)
- Yes/No 질문 (이직 가능 여부, 비자, 근무 가능 지역 등)
- 연봉 입력 필드 (희망 연봉, range, 총보상 기대치)
- 업로드 필드 (CV, 커버레터 PDF, 추천인 등)

각 질문을 분류합니다.
- **이미 블록 G에서 답변한 질문** -> 기존 답변을 가져와 조정
- **새 질문** -> report + `cv.md`를 바탕으로 답변 생성

## Step 5 -- 답변 생성

각 질문에 대해 다음 구조로 답변을 만듭니다.

1. **Report context**: 블록 B의 proof point와 블록 F의 STAR story 사용
2. **이전 블록 G**: 기존 초안이 있으면 base로 가져와 다듬기
3. **"제가 귀사를 선택합니다" 톤**: auto-pipeline과 같은 framework 사용 -- 자신감 있게, 애원하지 않게
4. **구체성**: 화면에 보이는 공고 내용 중 구체적인 요소 언급
5. **career-ops proof point**: "추가 정보" 같은 필드가 있으면 포함

**한국 지원서 폼에서 자주 나오는 필드:**
- **희망 연봉 / 처우** -> `profile.yml`의 range를 사용. 세전 연봉 기준인지 명확히 쓰고, "전체 보상 패키지에 따라 협의 가능"이라고 덧붙임
- **입사 가능일** -> 현재 재직/퇴사 통보 기간을 고려한 현실적인 날짜
- **근무 가능 지역 / 출근 가능 여부** -> 실제 가능한 지역과 출근 빈도를 명확히 작성
- **비자 / 근무 자격** -> 사실에 맞게 간결히 답변
- **언어 능력** -> 실제 수준에 맞춰 작성. 필요하면 CEFR 또는 비즈니스/실무 가능 수준으로 표현
- **포트폴리오 / GitHub / 블로그** -> `profile.yml`, `cv.md`, `article-digest.md`에 있는 검증된 링크만 사용

**출력 형식:**

```text
## [회사] -- [역할] 지원서 답변

Base: Report #NNN | Score: X.X/5 | Archetype: [type]

---

### 1. [지원서의 정확한 질문]
> [바로 복사/붙여넣기 가능한 답변]

### 2. [다음 질문]
> [답변]

...

---

Notes:
- [역할 변경, 공고 관련 관찰 등]
- [후보자가 직접 확인해야 할 개인화 포인트]
```


## Step 5b -- 폼에 직접 채워 넣기 (한국 ATS)

영문 `modes/apply.md`와 `docs/APPLY_AUTOFILL.md`에는 폼을 직접 타이핑하는 절이 있는데
한국어 번역에서 빠져 있었다. 한국 ATS를 실측해 여기에 채운다.

**절대 규칙: 제출하지 않는다.** 답변을 준비하고 선택지를 고르고 텍스트 칸을 채우는 것까지
한다. **제출 버튼은 언제나 후보자가 누른다.** 보내기 전에 검토할 기회를 남기는 것이다.

### 그리팅(greetinghr) — 국내 1위 ATS

`{회사}.career.greetinghr.com/ko/o/{공고번호}/apply`가 지원 폼 주소다.

실측(2026-09-15, 데상테코리아 공고 236287):

| 항목 | 값 | 자동 입력 |
|---|---|---|
| 총 필드 | 66개 | — |
| `input[text]` | 50개 | **가능** |
| `textarea` | 7개 | **가능** |
| `input[file]` | 2개 | 손대지 않는다 |
| `input[checkbox]` | 7개 | 손대지 않는다 |
| `react-select` | **0개** | 해당 없음 |

**로그인 없이 폼이 열린다.** 원티드는 로그인해야 폼이 보이지만 그리팅은 공개다.

**react-select가 없다.** 영문 문서가 Greenhouse·Ashby·Lever에서 경고한 "키 입력마다 DOM을
다시 만드는 드롭다운" 함정이 그리팅에는 없다. 한 글자씩 천천히 치는 우회가 필요하지 않다.

입력 방법은 React 제어 컴포넌트를 깨우는 표준 방식이다.

```js
const setter = Object.getOwnPropertyDescriptor(
  window.HTMLInputElement.prototype, 'value').set;
setter.call(input, 값);
input.dispatchEvent(new Event('input', { bubbles: true }));
```

`element.value = 값`만 쓰면 화면에는 보이지만 React 상태에 안 들어가 제출 때 빈 값으로 간다.
`textarea`는 `HTMLTextAreaElement.prototype`을 쓴다. 실측에서 둘 다 통했다.

### 한국 폼에서 클로드가 손대지 않는 칸

영문 규칙에 한국 폼 특성을 더한 것이다.

| 칸 | 이유 |
|---|---|
| 이름·영문이름·생년월일·연락처·이메일·주소 | 후보자 본인 정보다. 틀리면 연락이 안 온다 |
| **사진** | 파일 업로드이고, 채용절차법 제4조의3이 용모를 직무와 무관하게 요구하지 못하게 한다. 요구하는 폼을 만나면 그 사실만 알리고 판단은 후보자에게 넘긴다 |
| 성별·색약여부 등 신체 항목 | 같은 조문의 대상이다. 직무상 필요한 경우가 있으므로 단정하지 않고 알리기만 한다 |
| 희망 연봉·입사 가능일 | 협상 카드다. `profile.yml` 값을 제시하되 입력은 후보자가 확인하고 누른다 |
| 근무 자격·병역·동의 체크박스 | 사실 확인과 법적 동의다 |
| 파일 첨부 | 이력서·포트폴리오는 후보자가 고른다 |

### 채워 넣는 칸

- 서술형 답변(`textarea`) — 지원 동기, 자기소개, 경력 요약
- 경력·학력의 회사명·기간·직무처럼 `cv.md`에 근거가 있는 텍스트
- 특기·취미처럼 `profile.yml`이나 `cv.md`에서 확인되는 값

**근거 없는 칸은 비워 둔다.** 채우지 못한 칸을 목록으로 내놓고 후보자가 채우게 한다.

### 원티드

로그인해야 지원 폼이 열린다. 2026-09-15 실측에서 공개 API 100건 중 외부 지원(`is_outlink`)이
0건이었다 — 전부 원티드 자체 폼이다. 로그인 상태에서의 폼 구조는 확인하지 않았으므로,
처음 쓸 때는 한 칸만 채워 보고 값이 실제로 들어가는지 확인한 뒤 나머지를 진행한다.

### 채우고 나서

1. 채운 칸과 비워 둔 칸을 목록으로 내놓는다
2. 후보자가 검토한다
3. **후보자가 제출 버튼을 누른다**
4. 제출했다고 확인해 주면 Step 6으로 간다

## Step 6 -- 지원 후 처리 (선택)

후보자가 지원서를 제출했다고 확인하면:
1. tracker status 변경은 `data/applications.md`를 직접 수정하지 않습니다. `batch/tracker-additions/`에 `Applied` 상태의 TSV update를 쓰고 `node merge-tracker.mjs`로 병합합니다.
2. 최종 답변을 report의 블록 G에 업데이트합니다.
3. 다음 단계로 `/jobkit contacto`를 제안해 hiring manager 또는 recruiter에게 LinkedIn outreach를 할 수 있게 합니다.

## 스크롤 처리

지원서 폼에 화면에 보이는 것보다 질문이 더 많으면:
- 후보자에게 아래로 스크롤한 뒤 다른 스크린샷을 공유해달라고 요청합니다.
- 또는 남은 질문을 붙여넣어달라고 요청합니다.
- 전체 폼을 다룰 때까지 반복해서 처리합니다.
